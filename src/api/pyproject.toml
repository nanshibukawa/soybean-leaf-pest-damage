[project]
name = "api"
version = "0.1.0"
description = "FastAPI backend for soybean pest classification and RAG"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic-settings>=2.14.0",
    "fastembed>=0.4.2",
    "qdrant-client>=1.12.1",
]
